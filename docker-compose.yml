

services:
  r34sync:
    build: .
    container_name: r34sync_app
    ports:
      - "3000:3000"
    volumes:
      - ./r34_data:/app/data
    environment:
      - DATA_DIR=/app/data
    env_file:
      - .env
    restart: unless-stopped
